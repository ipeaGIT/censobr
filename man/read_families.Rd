% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_families.R
\name{read_families}
\alias{read_families}
\title{Download microdata of family records from Brazil's census}
\usage{
read_families(
  year = 2000,
  columns = NULL,
  as_data_frame = FALSE,
  showProgress = TRUE,
  cache = TRUE
)
}
\arguments{
\item{year}{Numeric. Year of reference in the format \code{yyyymm}. Defaults to \code{2000}.}

\item{columns}{String. A vector of column names to keep. The rest of the
columns are not read. Defaults to \code{NULL} and read all columns.}

\item{as_data_frame}{Logical. Whether the function should return a
\code{data.frame} or an Arrow Table (Default).}

\item{showProgress}{Logical. Defaults to \code{TRUE} display download progress
bar. The progress bar only reflects only the downloading
time, not the time to load the data to memory.}

\item{cache}{Logical. Whether the function should read the data cached
locally in future calls, which is much faster. Defaults to \code{TRUE}.
If \code{FALSE}, the function will download the file from the internet.}
}
\value{
An \verb{ArrowObject Dataset} or a \code{"data.frame"} object.
}
\description{
Download microdata of family records from Brazil's census. Data collected in
the sample component of the questionnaire.
}
\examples{
\dontshow{if (identical(tolower(Sys.getenv("NOT_CRAN")), "true")) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
# return data as arrow table
df <- read_families(year = 2000)
head(df)
\dontshow{\}) # examplesIf}
}
\seealso{
Other download microdata: 
\code{\link{read_deaths}()},
\code{\link{read_emmigration}()},
\code{\link{read_households}()},
\code{\link{read_population}()}
}
\concept{download microdata}
