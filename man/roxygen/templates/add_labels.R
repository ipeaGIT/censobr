#' @param add_labels Character. Whether the function should add labels to the
#'        responses of categorical variables. When `add_labels = "pt"`, the
#'        function adds labels in Portuguese. Defaults to `NULL`.
