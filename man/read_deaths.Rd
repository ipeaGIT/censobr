% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_deaths.R
\name{read_deaths}
\alias{read_deaths}
\title{Download microdata of death records from Brazil's census}
\usage{
read_deaths(
  year = 2010,
  columns = NULL,
  as_data_frame = FALSE,
  showProgress = TRUE,
  cache = TRUE
)
}
\arguments{
\item{year}{Numeric. Year of reference in the format \code{yyyymm}. Defaults to
\code{2010}.}

\item{columns}{String. A vector of column names to keep. The rest of the
columns are not read. Defaults to \code{NULL} and read all columns.}

\item{as_data_frame}{Logical. Whether the function should return a
\code{data.frame} or an Arrow Table (Default).}

\item{showProgress}{Logical. Defaults to \code{TRUE} display download progress
bar. The progress bar only reflects only the downloading
time, not the time to load the data to memory.}

\item{cache}{Logical. The first time the user runs the function, \code{censobr}
will download the file from the internet and store it locally so that
the file only needs to be download once. When the \code{cache} parameter is
set to \code{TRUE} (Default), the function will read the cached data, which
is much faster. If \code{FALSE}, the function will download the file from
the internet again and overwrite an eventual local file. The data is
stored locally at \code{tools::R_user_dir('censobr_v0.1', 'cache')}.}
}
\value{
An arrow \code{Dataset} or a \code{"data.frame"} object.
}
\description{
Download microdata of death records from Brazil's census. Data collected in
the sample component of the questionnaire.
}
\examples{
\dontshow{if (identical(tolower(Sys.getenv("NOT_CRAN")), "true")) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
# return data as arrow Dataset
df <- read_deaths(year = 2010)

# return data as data.frame
df <- read_deaths(year = 2010, as_data_frame = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
Other download microdata: 
\code{\link{read_emmigration}()},
\code{\link{read_families}()},
\code{\link{read_households}()},
\code{\link{read_population}()}
}
\concept{download microdata}
